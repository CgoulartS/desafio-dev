# Story 3.1: Dashboard com Metricas

## Status

Ready

## Executor Assignment

executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["python manage.py test", "python manage.py check"]

## Story

**As a** usuario do sistema,
**I want** ver um painel na pagina inicial com metricas do sistema,
**so that** eu tenha uma visao geral rapida da operacao.

## Acceptance Criteria

1. Home (`/`) exibe cards com: total de avioes, total de voos, total de clientes, total de reservas
2. Home exibe taxa de ocupacao media dos voos (reservas/capacidade * 100)
3. Home exibe lista dos proximos 5 voos (ordenados por data)
4. Dados calculados via context no HomeView (sem queries N+1)
5. Layout responsivo com Bootstrap cards
6. Testes para verificar que metricas sao calculadas corretamente (min. 3 testes)

## Complexity Estimate

**Size:** M (Medium)
**Points:** 5
**Justificativa:** Requer override de `get_context_data()` no HomeView com queries agregadas e logica de calculo de ocupacao media.

## Risks

- **Queries N+1:** Calcular ocupacao por voo pode gerar N+1 se nao otimizado. Mitigacao: usar `annotate()` com `Count` e `F()`.
- **Divisao por zero:** Voos sem capacidade ou sem voos no sistema. Mitigacao: tratar caso com `if total_capacidade > 0`.

## CodeRabbit Integration

> **CodeRabbit Integration**: Disabled — projeto academico, review manual suficiente.

## Tasks / Subtasks

- [ ] Task 1: Atualizar HomeView com metricas (AC: 1, 2, 3, 4)
  - [ ] Override `get_context_data()` no HomeView
  - [ ] Calcular `total_avioes = Aviao.objects.count()`
  - [ ] Calcular `total_voos = Voo.objects.count()`
  - [ ] Calcular `total_clientes = Cliente.objects.count()`
  - [ ] Calcular `total_reservas = Reserva.objects.count()`
  - [ ] Calcular taxa de ocupacao media usando `annotate` e `aggregate`
  - [ ] Buscar proximos 5 voos: `Voo.objects.filter(data__gte=date.today()).order_by('data', 'horario')[:5]`
- [ ] Task 2: Atualizar template home.html (AC: 1, 2, 3, 5)
  - [ ] Substituir cards estaticos por cards dinamicos com metricas
  - [ ] Card: Total de Avioes
  - [ ] Card: Total de Voos
  - [ ] Card: Total de Clientes
  - [ ] Card: Total de Reservas
  - [ ] Card/indicador: Taxa de Ocupacao Media (%)
  - [ ] Tabela: Proximos 5 Voos (origem, destino, data, horario, aviao)
- [ ] Task 3: Escrever testes (AC: 6)
  - [ ] Teste: metricas com banco vazio (zeros)
  - [ ] Teste: metricas com dados (contagens corretas)
  - [ ] Teste: taxa de ocupacao calculada corretamente

## Dev Notes

### get_context_data no HomeView

```python
from datetime import date
from django.db.models import Count, Sum, F, FloatField
from django.db.models.functions import Cast

class HomeView(LoginRequiredMixin, TemplateView):
    template_name = 'voos/home.html'

    def get_context_data(self, **kwargs):
        context = super().get_context_data(**kwargs)
        context['total_avioes'] = Aviao.objects.count()
        context['total_voos'] = Voo.objects.count()
        context['total_clientes'] = Cliente.objects.count()
        context['total_reservas'] = Reserva.objects.count()

        # Taxa de ocupacao media
        voos_com_dados = Voo.objects.annotate(
            num_reservas=Count('reservas')
        ).select_related('aviao')

        total_reservas = sum(v.num_reservas for v in voos_com_dados)
        total_capacidade = sum(v.aviao.max_passageiros for v in voos_com_dados)
        context['taxa_ocupacao'] = round((total_reservas / total_capacidade * 100), 1) if total_capacidade > 0 else 0

        # Proximos voos
        context['proximos_voos'] = Voo.objects.filter(
            data__gte=date.today()
        ).select_related('aviao').order_by('data', 'horario')[:5]

        return context
```

### Dependencia

- Todas as stories 1.x e 2.x ja implementadas
- HomeView ja existe (TemplateView) — apenas adicionar get_context_data

### Testing

- Framework: `django.test.TestCase`
- Local: `voos/tests.py`
- Min. 3 testes

### PRD References

- FR-14: Dashboard com metricas

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-04-06 | 1.0 | Story criada a partir do PRD Epic 3 | River (SM) |

## Dev Agent Record

### Agent Model Used
_Pending_

### Debug Log References
_Pending_

### Completion Notes List
_Pending_

### File List
_Pending_

## QA Results
_Pending_
