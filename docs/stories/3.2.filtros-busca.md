# Story 3.2: Filtros e Busca nas Listagens

## Status

Ready

## Executor Assignment

executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["python manage.py test", "python manage.py check"]

## Story

**As a** usuario do sistema,
**I want** filtrar e buscar registros nas listagens,
**so that** eu encontre rapidamente o que preciso.

## Acceptance Criteria

1. Lista de voos (`/voos/`) com campo de busca por origem ou destino
2. Lista de voos com filtro por data (input type="date")
3. Lista de clientes (`/clientes/`) com campo de busca por nome, email ou CPF
4. Busca via GET parameters (ex: `/voos/?q=GRU&data=2026-06-15`)
5. Filtros preservados na paginacao
6. Override de `get_queryset()` nas ListViews com filtro por `request.GET`
7. Testes para busca e filtro (min. 4 testes)

## Complexity Estimate

**Size:** M (Medium)
**Points:** 5
**Justificativa:** Override de get_queryset em 2 views + formulario de busca nos templates + preservacao de filtros na paginacao.

## Risks

- **SQL injection:** Usar `Q()` objects do Django ORM, nunca raw SQL. Mitigacao: Django ORM sanitiza automaticamente.
- **Busca vazia retorna tudo:** Comportamento desejado — sem filtro, lista completa.

## CodeRabbit Integration

> **CodeRabbit Integration**: Disabled — projeto academico, review manual suficiente.

## Tasks / Subtasks

- [ ] Task 1: Filtros na VooListView (AC: 1, 2, 4, 6)
  - [ ] Override `get_queryset()` em VooListView
  - [ ] Filtrar por `q` (icontains em origem e destino) usando `Q` objects
  - [ ] Filtrar por `data` (exact match em DateField)
  - [ ] Passar filtros no context via `get_context_data()`
- [ ] Task 2: Filtros na ClienteListView (AC: 3, 4, 6)
  - [ ] Override `get_queryset()` em ClienteListView
  - [ ] Filtrar por `q` (icontains em nome, email ou cpf) usando `Q` objects
  - [ ] Passar filtros no context
- [ ] Task 3: Atualizar templates de lista (AC: 1, 2, 3, 5)
  - [ ] Adicionar formulario de busca em `voo_list.html` (campo texto + campo data + botao)
  - [ ] Adicionar formulario de busca em `cliente_list.html` (campo texto + botao)
  - [ ] Manter valores dos filtros nos inputs (value="{{ request.GET.q }}")
- [ ] Task 4: Escrever testes (AC: 7)
  - [ ] Teste: buscar voo por origem
  - [ ] Teste: buscar voo por data
  - [ ] Teste: buscar cliente por nome
  - [ ] Teste: busca vazia retorna todos

## Dev Notes

### Override get_queryset com Q objects

```python
from django.db.models import Q

class VooListView(LoginRequiredMixin, ListView):
    model = Voo
    template_name = 'voos/voo_list.html'
    context_object_name = 'voos'
    paginate_by = 10  # Story 3.3

    def get_queryset(self):
        qs = super().get_queryset()
        q = self.request.GET.get('q', '')
        data = self.request.GET.get('data', '')
        if q:
            qs = qs.filter(Q(origem__icontains=q) | Q(destino__icontains=q))
        if data:
            qs = qs.filter(data=data)
        return qs

    def get_context_data(self, **kwargs):
        context = super().get_context_data(**kwargs)
        context['q'] = self.request.GET.get('q', '')
        context['data_filtro'] = self.request.GET.get('data', '')
        return context
```

### Template de busca (snippet)

```html
<form method="get" class="row g-2 mb-3">
    <div class="col-md-4">
        <input type="text" name="q" value="{{ q }}" class="form-control" placeholder="Buscar origem ou destino...">
    </div>
    <div class="col-md-3">
        <input type="date" name="data" value="{{ data_filtro }}" class="form-control">
    </div>
    <div class="col-md-2">
        <button type="submit" class="btn btn-primary">Buscar</button>
    </div>
</form>
```

### Dependencia

- Stories 1.x e 2.x implementadas
- VooListView e ClienteListView ja existem

### Testing

- Framework: `django.test.TestCase`
- Local: `voos/tests.py`
- Min. 4 testes
- Criar dados no setUp e buscar com GET params

### PRD References

- FR-15: Filtrar voos por origem, destino, data
- FR-16: Buscar clientes por nome, email, CPF

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-04-06 | 1.0 | Story criada a partir do PRD Epic 3 | River (SM) |

## Dev Agent Record

### Agent Model Used
_Pending_

### Debug Log References
_Pending_

### Completion Notes List
_Pending_

### File List
_Pending_

## QA Results
_Pending_
