# Story 3.3: Paginacao nas Listagens

## Status

Ready

## Executor Assignment

executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["python manage.py test", "python manage.py check"]

## Story

**As a** usuario do sistema,
**I want** que as listagens tenham paginacao,
**so that** o sistema funcione bem mesmo com muitos registros.

## Acceptance Criteria

1. Todas as 4 ListViews (avioes, voos, clientes, reservas) com `paginate_by = 10`
2. Componente de paginacao Bootstrap nos templates de lista
3. Paginacao preserva filtros de busca (query string mantida)
4. Links de primeira, anterior, proxima, ultima pagina
5. Indicador "Pagina X de Y"
6. Testes de paginacao (min. 2 testes)

## Complexity Estimate

**Size:** S (Small)
**Points:** 3
**Justificativa:** Adicionar `paginate_by` em 4 views + partial template de paginacao reutilizavel.

## Risks

- **Filtros perdidos na paginacao:** Links de pagina devem preservar `?q=...&data=...`. Mitigacao: template tag customizada ou construcao manual da query string.

## CodeRabbit Integration

> **CodeRabbit Integration**: Disabled — projeto academico, review manual suficiente.

## Tasks / Subtasks

- [ ] Task 1: Adicionar paginate_by nas ListViews (AC: 1)
  - [ ] `AviaoListView.paginate_by = 10`
  - [ ] `VooListView.paginate_by = 10`
  - [ ] `ClienteListView.paginate_by = 10`
  - [ ] `ReservaListView.paginate_by = 10`
- [ ] Task 2: Criar partial template de paginacao (AC: 2, 3, 4, 5)
  - [ ] Criar `templates/voos/includes/pagination.html`
  - [ ] Links: primeira, anterior, numeros, proxima, ultima
  - [ ] Indicador "Pagina X de Y"
  - [ ] Preservar query string nos links (construir href com parametros GET existentes)
  - [ ] Incluir partial em todos os 4 templates de lista com `{% include %}`
- [ ] Task 3: Escrever testes (AC: 6)
  - [ ] Teste: criar 15 avioes, verificar que pagina 1 tem 10 e pagina 2 tem 5
  - [ ] Teste: paginacao retorna 404 para pagina invalida ou vazia graceful

## Dev Notes

### Partial de paginacao (pagination.html)

```html
{% if page_obj.has_other_pages %}
<nav>
  <ul class="pagination justify-content-center">
    {% if page_obj.has_previous %}
    <li class="page-item"><a class="page-link" href="?page=1{% for k,v in request.GET.items %}{% if k != 'page' %}&{{ k }}={{ v }}{% endif %}{% endfor %}">Primeira</a></li>
    <li class="page-item"><a class="page-link" href="?page={{ page_obj.previous_page_number }}{% for k,v in request.GET.items %}{% if k != 'page' %}&{{ k }}={{ v }}{% endif %}{% endfor %}">Anterior</a></li>
    {% endif %}
    <li class="page-item disabled"><span class="page-link">Pagina {{ page_obj.number }} de {{ page_obj.paginator.num_pages }}</span></li>
    {% if page_obj.has_next %}
    <li class="page-item"><a class="page-link" href="?page={{ page_obj.next_page_number }}{% for k,v in request.GET.items %}{% if k != 'page' %}&{{ k }}={{ v }}{% endif %}{% endfor %}">Proxima</a></li>
    <li class="page-item"><a class="page-link" href="?page={{ page_obj.paginator.num_pages }}{% for k,v in request.GET.items %}{% if k != 'page' %}&{{ k }}={{ v }}{% endif %}{% endfor %}">Ultima</a></li>
    {% endif %}
  </ul>
</nav>
{% endif %}
```

### Dependencia

- ListViews ja existem (Stories 1.3, 2.1, 2.2, 2.3)
- Filtros de busca (Story 3.2) devem ser preservados

### Testing

- Framework: `django.test.TestCase`
- Local: `voos/tests.py`
- Min. 2 testes
- Usar loop para criar registros em massa no setUp

### PRD References

- FR-17: Paginacao (10 itens por pagina)

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-04-06 | 1.0 | Story criada a partir do PRD Epic 3 | River (SM) |

## Dev Agent Record

### Agent Model Used
_Pending_

### Debug Log References
_Pending_

### Completion Notes List
_Pending_

### File List
_Pending_

## QA Results
_Pending_
